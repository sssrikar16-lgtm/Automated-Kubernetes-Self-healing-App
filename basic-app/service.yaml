apiVersion: v1
kind: Service
metadata:
  name: basic-app-service
  namespace: self-healing
  labels:
    app: basic-app
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  selector:
    app: basic-app
